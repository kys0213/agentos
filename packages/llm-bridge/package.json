{
  "name": "@agentos/llm-bridge",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@agentos/llm-bridge-spec": "workspace:*",
    "@agentos/shared": "workspace:*",
    "json-schema-to-zod": "^2.6.1",
    "openai": "^4.28.0",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@agentos/llama3-llm-bridge": "workspace:*",
    "@types/node": "^22.14.1",
    "@vitest/coverage-v8": "^1.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
