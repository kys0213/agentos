import React from 'react';

/**
 * 커맨드 팔레트 컴포넌트 (임시 간소화)
 * - store 의존성 제거하여 무한 렌더링 방지
 * - Week 2 UX 기능 테스트 후 복원 예정
 */
const CommandPalette: React.FC = () => {
  // 임시로 빈 컴포넌트 반환
  // kbar와 store 의존성을 모두 제거
  console.log('CommandPalette: Temporarily disabled to prevent infinite rendering');

  return null;
};

export default CommandPalette;
